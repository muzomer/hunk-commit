{
  "name": "hunk-stage",
  "version": "0.1.0",
  "description": "Mark hunks while reviewing in Hunk and stage them — into the git index, or into a Jujutsu revision",
  "license": "MIT",
  "type": "module",
  "hunk": {
    "extensions": [
      "./index.ts"
    ],
    "apiVersion": 8
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "hunkdiff": "^0.20.0",
    "typescript": "^5"
  }
}
